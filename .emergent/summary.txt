<analysis>**original_problem_statement:**
The user wants to build a fully autonomous AI-to-AI Venture Capital (VC) fund platform, Anima Fund. This involves a multi-agent platform to create, monitor, and manage multiple independent AI agents from a single UI. Each agent must be fully autonomous, operating in its own sandboxed VM on the live internet.

**PRODUCT REQUIREMENTS:**
-   **Core Functionality:** A multi-agent platform to create, monitor, and manage independent AI agents from a single dashboard. Each agent has its own VM, wallet, and skills.
-   **Agent Autonomy & Lifecycle:**
    1.  The agent must be created and operate *exclusively within* a sandboxed VM, not on the host.
    2.  The agent's operations are governed by a strict, mechanically-enforced 4-phase prompt:
        -   **Phase 0 (Tooling & Testing):** Verify all core tools are functional.
        -   **Phase 1 (Capital Acquisition):** Make **,000 within one hour** by all means necessary. Forbidden from building any product or service.
        -   **Phase 2 (Capital Growth):** Grow capital to **0,000**.
        -   **Phase 3 (Business Incubation & Fund):** After reaching 0k, the agent can begin building its product, start the Anima Fund, and use tools like **Boardy.ai**.
-   **Real-time Dashboard:** A dashboard to monitor all agents, with real-time logs and terminal/browser views for each Anima VM.
-   **User Control:** The user provisions the agent's tools via dashboard controls but does not control the agent's actions once it goes autonomous.

**User's preferred language**: English

**what currently exists?**
The application is a React/FastAPI/MongoDB stack. The architecture has been significantly refactored to be **strictly sandbox-only**. All agent creation, provisioning, and operational logic now executes exclusively within a Conway sandbox VM, with the backend acting as a secure proxy to the Conway API. Host-side execution has been eliminated. The UI has been redesigned to make the multi-step provisioning flow the primary onboarding experience on the main landing page, fixing a critical bug that allowed users to bypass the sandbox. The  page is now a dedicated monitoring-only dashboard.

**Last working item**:
-   **Last item agent was working:** The agent was investigating how to implement a payment mechanism for Conway API credits. The user identified a critical blocker: the Create Sandbox step requires credits, but there is no UI to fund them. The agent had just found the  Conway endpoint and was about to determine how to use it to generate a payment QR code for the user.
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N
-   **Which testing method agent to use?** both
-   **User Testing Done:** N

**All Pending/In progress Issue list**:
-   **Issue 1 (P0): Implement Conway Credits Funding Mechanism.** This is the top priority and a major blocker for the entire application.

**Issues Detail:**
-   **Issue 1: Implement Conway Credits Funding Mechanism.**
    -   **Attempted fixes:** The agent identified the  endpoint in the Conway API but did not implement the full flow.
    -   **Next debug checklist:**
        1.  Create a new backend endpoint (e.g., ) that calls Conway's  to get payment details (USDC address, amount).
        2.  Create a new backend endpoint (e.g., ) to fetch the current Conway API key credit balance.
        3.  Modify the genesis screen in  to add a Step 0: Fund Account section before the current provisioning stepper.
        4.  This new section should display the current credit balance and a button to Add Credits.
        5.  Clicking the button should call the new funding endpoint and display the payment information as a QR code.
        6.  The Create Sandbox step must remain disabled until the credit balance is sufficient to pay for a sandbox VM.
    -   **Why fix this issue and what will be achieved with the fix?** This will solve the critical chicken-and-egg problem of needing credits to start the provisioning process. It will unblock the entire agent creation flow by allowing the user to fund their account directly from the UI.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** N
    -   **Should Test frontend/backend/both after fix?** both
    -   **Blocked on other issue:** None. This is the primary blocker.

**In progress Task List**:
-   None.

**Upcoming and Future Tasks**
-   **Upcoming Tasks:**
    -   (P1) Update  to instruct the agent to monitor and top up its own Conway credits using its x402 wallet, making it truly self-sustaining.
-   **Future Tasks:**
    -   Implement Real Smart Contracts.
    -   Integrate Android Device Control.
    -   Migrate to a self-hosted agent engine.

**Completed work in this session**
-   **Massive Host-Side Logic Removal:** Rewrote the entire backend (, , , etc.) to eliminate all dependencies on the host machine. All agent operations now run exclusively within the Conway sandbox.
-   **UI Flow Refactor (Onboarding):** Moved the multi-step provisioning UI to the main genesis/onboarding screen in . The  page is now a monitoring-only dashboard. This fixed the critical bug where a user could create an agent on the host.
-   **Conway API Alignment & Skill Discovery Fix:** Audited and updated the backend against the latest Conway docs. Corrected the skill loading path () and updated the genesis prompt to instruct the agent on how to discover and install skills from ClawHub.
-   **Deployment Readiness:** Fixed the  file to stop blocking  files and successfully ran the .
-   **Comprehensive E2E Testing:** Ran the testing agent multiple times after each major refactor, achieving 100% pass rates and verifying all architectural changes.

**Earlier issues found/mentioned but not fixed**
-   None.

**Known issue recurrence from previous fork**
-   None.

**Code Architecture**


**Key Technical Concepts**
-   **Sandbox-First Architecture:** All agent operations MUST execute within the Conway sandbox VM. The backend is a stateless proxy to the Conway API.
-   **UI-Driven Onboarding:** The initial user experience is a multi-step provisioning process on the main landing screen.
-   **Creator vs. Agent Funding:**
    -   **Conway Credits (Creator):** Pays for the sandbox VM. Must be funded *before* provisioning.
    -   **x402 Wallet (Agent):** Pays for the agent's own operational costs (e.g., domains). Created *inside* the sandbox.

**key DB schema**
-   **agents**: 

**changes in tech stack**
-   None.

**All files of reference**
-   : Logic for the onboarding flow. Needs modification for credit funding.
-   : Contains the onboarding/provisioning stepper. Will be modified to add the credit funding UI.
-   : Logic for the individual provisioning steps.
-   : The agent's core mission document.

**Areas that need refactoring**:
-   None. The codebase just underwent a massive refactoring.

**key api endpoints**
-   : Checks if an agent is provisioned.
-   : The first step in provisioning.
-   **NEEDS TO BE CREATED:**  to get a payment QR for Conway credits.
-   **NEEDS TO BE CREATED:**  to fetch the current Conway API key credit balance.

**Critical Info for New Agent**
-   **Your top priority is implementing the Conway credits funding mechanism.** The entire application is blocked until the user can pay for the initial sandbox creation. You must implement the full backend and frontend flow to allow users to add credits to their Conway API key via a QR code.
-   **The architecture is now STRICTLY sandbox-only.** Do not introduce any logic that interacts with the host filesystem or runs host processes.
-   Do not confuse the two separate payment systems: **Conway Credits** (for compute, paid by the creator) and the **Agent's x402 Wallet** (for services, paid by the agent). Your current task is about funding the former.

**documents and test reports created in this job**
-   
-   Multiple test reports in .

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Pointed out the critical bug: I deployed and launched the agent without provisioning first.
2.  **User:** Gave clear direction to move the provisioning stepper to the onboarding screen.
3.  **User:** Corrected the agent's first attempt at the UI refactor.
4.  **User:** Triggered the major sandbox-only audit by asking for verification of the agent's environment.
5.  **User:** Stated the core principle: There should be absolutely nothing on host.
6.  **Agent:** Performed the massive refactor to make everything sandbox-only.
7.  **User:** Identified the payment chicken-and-egg problem.
8.  **User:** Confirmed the agent should be self-sustaining.
9.  **User (pending):** you need to add an addional QR code to fund the sandbox for step 1... that funds the conway credits using the conway api? double check for the best way to do this - **This is the current task.**

**Project Health Check:**
-   **Broken:** The entire user flow is blocked because there is no way to fund the Conway API key credits required to create the initial sandbox.
-   **Mocked:** None.

**3rd Party Integrations**
-   Conway Research Ecosystem (web4.ai): Core dependency for sandboxed VMs, terminal, tool installation, and payments.
-   OpenClaw: For autonomous web browsing.
-   Claude Code: For agent self-modification.
-   Telegram Bot API

**Testing status**
-   **Testing agent used after significant changes:** YES. The last major refactor was fully tested.
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None.
-   **Known regressions:** None.

**Credentials to test flow:**
-   The  in the backend  file currently has **0 credits**. This is essential for testing the new funding flow.

**What agent forgot to execute**
-   The agent initially missed the payment dependency for sandbox creation until the user pointed it out. Implementing the credit funding mechanism is the main pending task.</analysis>
