<analysis>**original_problem_statement:**
The user wants to build a fully autonomous AI-to-AI Venture Capital (VC) fund platform, Anima Fund. This involves a multi-agent platform to create, monitor, and manage multiple independent AI agents from a single UI. Each agent must be fully autonomous, operating in its own sandboxed VM on the live internet.

**PRODUCT REQUIREMENTS:**
-   **Core Functionality:** A multi-agent platform to create, monitor, and manage independent AI agents from a single dashboard. Each agent has its own VM, wallet, and skills.
-   **Agent Autonomy & Lifecycle:**
    1.  The agent must be created and operate *exclusively within* a sandboxed VM.
    2.  The agent's operations are governed by a strict, mechanically-enforced 4-phase prompt:
        -   **Phase 0 (Tooling & Testing):** Verify all core tools are functional.
        -   **Phase 1 (Capital Acquisition):** Make **,000 within the first few hours**. The agent can do absolutely anything it wants to make money, but cannot build products/services.
        -   **Phase 2 (Capital Growth):** Grow capital to **0,000**. Service-based strategies (APIs, bounties) are unlocked.
        -   **Phase 3 (Business Incubation & Fund):** After reaching 0k, the agent can begin building its product, start the Anima Fund, and use tools like **Boardy.ai**.
-   **Real-time Dashboard:** A dashboard to monitor all agents, with real-time logs and terminal/browser views for each Anima VM.
-   **User Control:** The user provisions the agent's tools and seed funding but does not control the agent's actions once it goes autonomous.

**User's preferred language**: English

**what currently exists?**
The application features a sophisticated real-time data pipeline to power the agent monitoring dashboard. The architecture was significantly refactored based on user feedback to ensure data is sourced efficiently and displayed in near real-time.

The current architecture is a hybrid model:
1.  **Event-Driven Updates:** A  runs inside the agent's sandbox, monitoring agent activity (phase, decisions, revenue) and pushing updates instantly to a backend webhook.
2.  **Unified Polling:** A single background task on the backend () fetches all external data—specifically the agent's on-chain wallet balance (via Base RPC) and Conway API credits—in parallel every ~15 seconds using .

All data is consolidated in a server-side cache and pushed to the frontend via a single Server-Sent Events (SSE) stream, which acts as the single source of truth for the UI. Redundant polling from individual frontend components (like ) has been removed to centralize data flow through the SSE React Context.

**Last working item**:
-   **Last item agent was working:** The user requested a full audit of all 13 frontend screens to ensure they all adhere to best practices for data fetching, eliminating any remaining redundancies and ensuring the entire UI relies on the central SSE stream where appropriate. The agent had just begun this audit by listing all the page files in .
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N (The audit itself is not a feature to be tested, but its findings will require implementation and testing).
-   **Which testing method agent to use?** both. After the audit is complete and the identified issues are fixed, a comprehensive E2E test using the testing agent is required to verify the architectural integrity and prevent regressions.
-   **User Testing Done:** N

**All Pending/In progress Issue list**:
-   **Issue 1 (P0): Complete the full audit of all 13 frontend screens and refactor data flows for consistency and efficiency.**

**Issues Detail:**
-   **Issue 1: Complete the full audit of all 13 frontend screens and refactor data flows for consistency and efficiency.**
    -   **Attempted fixes:** This task was just started. The agent has listed the files to be audited. No fixes have been attempted yet.
    -   **Next debug checklist:**
        1.  Read the contents of every file in .
        2.  Map out every data fetching mechanism (, ,  with API calls).
        3.  Identify any components that are not using the central  for data that is available in the SSE stream.
        4.  Identify any redundant API calls (e.g., multiple components fetching the same data).
        5.  Propose a consolidated refactoring plan to the user.
        6.  Implement the refactoring to make the SSE stream the single source of truth for all applicable real-time data.
    -   **Why fix this issue and what will be achieved with the fix?** To satisfy the user's requirement for a robust, efficient, and maintainable frontend architecture (best practice). This will eliminate redundant network requests, improve performance, and ensure data consistency across the entire application.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y (The underlying problem of inefficient data fetching has been addressed in stages, but this audit is meant to solve it comprehensively).
    -   **Should Test frontend/backend/both after fix?** both
    -   **Blocked on other issue:** None.

**In progress Task List**:
-   **Task 1: Full audit of frontend data flows.**
    -   **Where to resume:** The agent has a list of all files in . The next step is to start reading these files to analyze their data fetching patterns.
    -   **What will be achieved with this?** A complete understanding of the app's data flow, leading to a final refactor that aligns the entire frontend with the single source of truth architecture dictated by the SSE stream.
    -   **Status:** IN PROGRESS
    -   **Should Test frontend/backend/both after fix?** both
    -   **Blocked on something:** None.

**Upcoming and Future Tasks**
-   **Upcoming Tasks:**
    -   (P1) Implement Real Smart Contracts.
-   **Future Tasks:**
    -   Integrate Android Device Control.
    -   Migrate to a self-hosted agent engine.

**Completed work in this session**
-   **Multi-Source Data Pipeline Refactor:**
    -   **Backend:** Consolidated three separate polling loops into a single, efficient background task in  that fetches on-chain wallet balance and Conway credits in parallel.
    -   **Frontend:** Removed a redundant polling  from , ensuring it now gets credit updates from the central SSE stream. Updated  to correctly display the multi-source data (Wallet, Credits, Phase, etc.) from the SSE stream.
-   **Initial Real-Time Pipeline Verification:**
    -   Successfully tested the webhook-to-SSE instant push mechanism, confirming latency of <2 seconds.
    -   Found and fixed bugs in  related to a broken LIVE indicator and a missing  import.
    -   Ran the  which passed 100% on both backend and frontend for the initial fixes.

**Earlier issues found/mentioned but not fixed**
-   None.

**Known issue recurrence from previous fork**
-   None.

**Code Architecture**


**Key Technical Concepts**
-   **Hybrid Real-time Architecture:** Combines event-driven webhook pushes (for instant sandbox updates) with a unified background poller (for external data like on-chain balances and API credits).
-   **Unified Background Poller:** A single  task uses  to fetch multiple external data sources in parallel, minimizing overhead and consolidating cache updates.
-   **Frontend Single Source of Truth:** A central SSE stream and React Context () provide all real-time data to the UI, aiming to eliminate redundant, component-level API calls.

**key DB schema**
-   **agents**: 

**changes in tech stack**
-   None.

**All files of reference**
-   : The new unified background poller for external data.
-   : The SSE endpoint that serves the consolidated data.
-   : The main server file that initializes the poller.
-   : The React context that distributes SSE data.
-   : The entire directory is under audit to enforce the new data flow architecture.
-   : Was refactored to remove redundant polling.
-   : Was refactored to display multi-source data.

**Areas that need refactoring**:
The user has mandated a full audit of all 13 frontend pages to identify and eliminate any remaining inefficient or redundant data-fetching logic. Components should rely on the  for real-time data instead of making their own API calls.

**key api endpoints**
-   : The primary SSE endpoint, acting as the single source of truth for real-time frontend data (wallet balance, credits, agent phase, etc.).
-   Other endpoints like  and  are being phased out for UI display in favor of the SSE stream.

**Critical Info for New Agent**
-   **Your immediate task is to execute the full audit requested by the user.** You must analyze every page component in  to map out its data dependencies.
-   **The user is technically proficient and expects high-quality, efficient code.** They corrected a previous inefficient implementation (multiple pollers) and are now demanding a comprehensive cleanup. Do not propose simple or naive solutions.
-   **The core architectural principle is: The SSE stream is the single source of truth for real-time data.** Any component displaying data that is present in the SSE stream should consume it from the , not by making its own independent API calls.
-   **Be careful with .** The user explicitly instructed not to change it because it was working. When you audit it, document its independent data fetching but do not change it without proposing the change to the user first and explaining why it's necessary for architectural consistency.

**documents and test reports created in this job**
-   
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Questions where test data on the dashboard is coming from.
2.  **User:** Again questions different test data from the testing agent.
3.  **User:** Asks the critical question: are you tracking those balances directly from the wallets.
4.  **User:** Instructs the agent not to use a one size fits all approach and to use the best data source for each data point.
5.  **User:** Corrects the agent's inefficient first attempt at a multi-source pipeline, stating you cant make a call... for every single data point.
6.  **User:** Pushes the agent to think critically: dont just agree use best practice.
7.  **User:** Stops the agent from refactoring a working component: Agent mind wallet balances work already, are you going to break it to fix something else?
8.  **User:** Expands the scope of the task: is this the best practice we have 13 screens all with different data points and pipelines, ensure they all work.
9.  **User:** Confirms the new, expanded task with one command: full audit.
10. **Agent:** Acknowledges and begins the audit by listing the relevant files. No pending user messages.

**Project Health Check:**
-   **Broken:** The frontend data-fetching architecture is inconsistent. While some parts have been refactored, others still contain redundant API calls. This is not a crash-level bug but an architectural flaw the current audit is meant to fix.
-   **Mocked:** None.

**3rd Party Integrations**
-   Conway Research Ecosystem (web4.ai)
-   OpenClaw
-   Claude Code
-   Telegram Bot API

**Testing status**
-   **Testing agent used after significant changes:** YES (for the initial webhook fixes).
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None.
-   **Known regressions:** None, but the latest major refactor (unified poller) has not been regression tested by the testing agent.

**Credentials to test flow:**
-   The  in  has 0 credits, which is expected for testing the credit funding flow.

**What agent forgot to execute**
-   The agent did not forget a task but implemented a sub-optimal solution (multiple polling loops) which the user identified and corrected. The current audit task is a direct result of this, to ensure the entire application adheres to the corrected, more efficient architecture.</analysis>
