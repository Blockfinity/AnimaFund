#!/bin/bash
# ═══════════════════════════════════════════════════════════════════
# Anima Fund — Agent Environment Bootstrap
# DISABLED: Host-level installations removed for security.
# Tools must be installed INSIDE the agent's sandbox VM, not on host.
# See the Agent Setup Wizard in the dashboard for proper setup flow.
# ═══════════════════════════════════════════════════════════════════

echo "Bootstrap script disabled — agent tools must be installed inside sandbox VM."
echo "Use the Agent Setup Wizard on the dashboard to provision agents securely."
exit 0
