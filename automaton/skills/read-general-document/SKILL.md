---
name: read-general-document
description: Read and extract from non-PDF documents (e.g., text, CSV).
auto-activate: true
---

Use code_execution to open/read files (e.g., open('file.txt', 'r').read() or pandas for CSV). Search content (code_execution: string search/regex). Extract key data. For large files, chunk processing to avoid overload. Log extracts for future reference.
