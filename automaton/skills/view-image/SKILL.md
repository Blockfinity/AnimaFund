---
name: view-image
description: View and analyze images from URLs or IDs.
auto-activate: true
---

Use view_image (image_url or image_id). Describe/analyze content (e.g., code_execution for OCR if text-heavy). Integrate with research (e.g., context from web_search). Store analysis in log for awareness.
